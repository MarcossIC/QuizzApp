---
server:
  error:
    #Al haber un error envia siempre message y los parametros
    include-message: always
    include-binding-errors: always
logging:
  level:
    root: INFO
    web: DEBUG
    org:
      hibernate:
        SQL: DEBUG
        type:
          descriptor:
            sql:
              BasicBinder: TRACE
  pattern:
    dateformat: HH:mm:ss


spring:
  main:
    banner-mode: off
    logging:
      level:
        org:
          springframework:
            data:
              r2dbc: DEBUG
    server:
      error:
        include-stacktrace: on_param
  r2dbc:
    url: r2dbc:postgresql://db.eqauodziqzmxgqruirui.supabase.co:5432/postgres
    username: postgres
    password: WriteOnceRunEverywhere

...